spring:
      application:
         name: resilenceApp
      profiles:
         active: dev
      cloud:
         config:
            uri: http://localhost:8888
            enabled: true 
server:
  port: 9090
  
logging:
  file: resilence.log